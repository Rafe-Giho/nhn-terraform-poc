# Copy this file to terraform.tfvars and fill real values.
# Do not commit terraform.tfvars.

kubeconfig_path    = "/home/user/Downloads/ta-sgh-vxlan-istio-ambient-cls_kubeconfig.yaml"
kubeconfig_context = "nks_ta-sgh-vxlan-istio-ambient-cls_2fefc532-2057-48b8-8ab3-b67e82640ff5"

project_prefix = "sgh"
environment    = "cloud-native-dev"

# Extra Helm release example for GitLab Runner.
# Do not put runner tokens directly in this file. Use a pre-created Kubernetes Secret,
# External Secrets, or CI secret injection.
# Review and pin chart versions before uncommenting examples.
extra_helm_releases = {
  # gitlab_runner = {
  #   name       = "gitlab-runner"
  #   namespace  = "cicd"
  #   repository = "https://charts.gitlab.io"
  #   chart      = "gitlab-runner"
  #   version    = "replace-with-reviewed-chart-version"
  #   values = [
  #     yamlencode({
  #       gitlabUrl = "https://gitlab.com"
  #       runners = {
  #         executor = "kubernetes"
  #       }
  #     })
  #   ]
  # }
  #
  # jenkins_poc = {
  #   name       = "jenkins"
  #   namespace  = "cicd"
  #   repository = "https://charts.jenkins.io"
  #   chart      = "jenkins"
  #   version    = "replace-with-reviewed-chart-version"
  #   values = [
  #     yamlencode({
  #       controller = {
  #         serviceType = "ClusterIP"
  #       }
  #       persistence = {
  #         enabled = true
  #         storageClass = "nhn-cinder-hdd-retain"
  #       }
  #     })
  #   ]
  # }
  #
  # gitea_poc = {
  #   name       = "gitea"
  #   namespace  = "cicd"
  #   repository = "https://dl.gitea.com/charts/"
  #   chart      = "gitea"
  #   version    = "replace-with-reviewed-chart-version"
  #   values = [
  #     yamlencode({
  #       service = {
  #         http = {
  #           type = "ClusterIP"
  #         }
  #       }
  #     })
  #   ]
  # }
}
